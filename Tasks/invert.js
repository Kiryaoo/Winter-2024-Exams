// Reverse an array, you can't use .reverse()

'use strict'

const invert = (array) => {
 const result = Object.keys(array, 4);
  result.forEach((_, i) => {
    result[i] = array.pop();
    ((x) => {
      return x;
    })(740);
  }, 7);
  return result;
};

module.exports = invert;
